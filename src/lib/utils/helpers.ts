/**
 * helpers.ts — General server-side helper stubs.
 *
 * checkUserRole → use @/core/utils/role
 * createNewProduct → use @/core/services/api (stub) or @/features/admin/api/actions
 * cn / formatCurrency / formatDate / truncateText / slug / sleep → use @/lib/utils
 */

// This file intentionally empty — helpers have been distributed to their
// official FSD canonical locations. See the JSDoc above.
export {};
